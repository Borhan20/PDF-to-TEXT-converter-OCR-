version: '3.8'

services:
  ocr-pdf:
    build: .
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: ["tail", "-f", "/dev/null"] # Keep the container running for dev
